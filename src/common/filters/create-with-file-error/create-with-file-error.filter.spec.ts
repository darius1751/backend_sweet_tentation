import { CreateWithFileErrorFilter } from './create-with-file-error.filter';

describe('CreateWithFileErrorFilter', () => {
  it('should be defined', () => {
    expect(new CreateWithFileErrorFilter()).toBeDefined();
  });
});
